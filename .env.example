# ============================================================================
# InternBoot Environment Configuration (.env.example)
#
# Copy this file to .env and configure for your environment:
#   cp .env.example .env
#
# STRICT WARNING: Never commit .env to version control! It is in .gitignore.
# ============================================================================

# Environment mode: 'production' or 'development'
# In production, detailed database error traces are suppressed from client responses.
APP_ENV=production

# Application Base URL (Required for building absolute links in emails, e.g. password reset)
# In production, set to your actual domain with HTTPS (e.g. https://internboot.com).
# In local development, set to your local host (e.g. http://localhost:8000).
APP_URL=https://yourdomain.com

# ============================================================================
# Database Configuration (MySQL / Railway Cloud Instance)
# ============================================================================
# Explicit DB connection credentials (override Railway defaults when set):
DB_HOST=your-project.proxy.rlwy.net
DB_PORT=12345
DB_USER=root
DB_PASSWORD=your_database_password_here
DB_NAME=railway

# Railway-provided MySQL platform variables (injected automatically in Railway):
MYSQL_DATABASE=railway
MYSQL_PUBLIC_URL=mysql://root:your_database_password_here@your-project.proxy.rlwy.net:12345/railway
MYSQL_ROOT_PASSWORD=your_database_password_here
MYSQL_URL=mysql://root:your_database_password_here@mysql.railway.internal:3306/railway
MYSQLDATABASE=railway
MYSQLHOST=mysql.railway.internal
MYSQLPASSWORD=your_database_password_here
MYSQLPORT=3306
MYSQLUSER=root

# ============================================================================
# DEV / DEMO ONLY — DO NOT ENABLE IN PRODUCTION
# ============================================================================
# Demo/test mode bypasses candidate authentication and real payment flows for
# local testing ONLY. MUST be 0 (or unset) in production.
M4_DEMO_MODE=0

# Secret key required for demo payment/candidate-impersonation fail-safe guard.
# When M4_DEMO_MODE=1 in non-production, requests must supply a matching demo_secret.
# Must be a long random string, generated per-environment, never reused, and NEVER enabled in production.
M4_DEMO_SECRET=generate_a_long_random_secret_here

# ============================================================================
# SMTP Mail Configuration (M3 Auth OTP & Password Reset Emails)
# Replace placeholders with real SMTP credentials (e.g. SendGrid, AWS SES, Mailtrap)
# ============================================================================
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USERNAME=your_smtp_username
MAIL_PASSWORD=your_smtp_password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@yourdomain.com
MAIL_FROM_NAME="InternBoot"

# ============================================================================
# Payment Gateway Configuration (M4 Payment Integration - Easebuzz / PayU)
# ============================================================================
KEY=your_merchant_key
SALT=your_merchant_salt

# ============================================================================
# AI Provider Configuration (M1 AI Question Bank Generation)
# ============================================================================
AI_PROVIDER=gemini
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.0-flash
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini

# ============================================================================
# First-Time Database Seed (scripts/seed.php)
# Run once after applying the schema to bootstrap an initial admin account,
# assessment, and question bank: php scripts/seed.php
# ============================================================================
SEED_ADMIN_EMAIL=admin@yourdomain.com
SEED_ADMIN_PASSWORD=ChangeMe_AtLeast8Chars!
SEED_ADMIN_NAME=Platform Admin

# Optional seed parameters (defaults shown below are used when unset):
# SEED_ASSESSMENT_TITLE=InternBoot Level Assessment
# SEED_ASSESSMENT_DURATION=60
# SEED_ASSESSMENT_QUESTIONS=50
# SEED_QBANK_NAME=Main Question Bank
